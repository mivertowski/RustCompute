% memory.tex — Memory Model
\section{Memory Model}
\label{sec:memory}

Stub: GPU memory hierarchy (registers $\to$ shared $\to$ DSMEM
$\to$ L1/L2 $\to$ HBM), persistent state placement, mapped-memory
control blocks, the actor's view as constrained by Lustig's
axiomatic model. Connects \Cref{def:actor-state} to physical
memory.
